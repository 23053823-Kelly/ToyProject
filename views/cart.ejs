<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shopping Cart</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <%- include('partials/navbar.ejs') %> 
    <div class="container mt-3">
        <h2>Shopping Cart</h2>
        <p>You can only buy one item at a time!</p>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Toy ID</th>
                    <th>Toy Name</th>
                    <th>Image</th>
                    <th>Age Range</th>
                    <th>Quantity</th>
                    <th>Price</th>
                    
                </tr>
            </thead>
            <tbody>
                <% for (let i = 0; i < cartItems.length; i++) { %>
                    <tr>
                        <td><%= cartItems[i].toyid %></td>
                        <td><%= cartItems[i].productName %></td>
                        <td><img src="/images/<%= cartItems[i].image %>" alt="Toy Image" width="100"></td>
                        <td><%= cartItems[i].agegroup %></td>
                        <td><%= cartItems[i].quantity %></td>
                        <td>$<%= cartItems[i].price %></td>
                        
                    </tr>
                <% } %>
            </tbody>
        </table>
        <div class="text-end">
            <a href="/checkout" class="btn btn-primary">Proceed to Checkout</a>
            <a href="/" class="btn btn-secondary ms-2">Continue Shopping</a>
        </div>
        
    </div>
</body>
</html>

