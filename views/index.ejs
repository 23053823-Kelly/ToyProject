<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/main.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Toy App</title>
    
</head>
<body>
    <%- include('partials/navbar.ejs') %>
    
    <div class="container mt-3">
        <h2>Toy Inventory</h2>
                
        <table class="table table-striped">
            <thead>
                <tr>
                    <th class="bg-secondary">Toy ID</th>
                    <th class="bg-secondary">Toy Name</th>
                    <th class="bg-secondary">Image</th>
                    <th class="bg-secondary">Age Range</th>
                    <th class="bg-secondary">Quantity</th>
                    <th class="bg-secondary">Price</th>
                    <th class="bg-secondary">Edit</th>
                    <th class="bg-secondary">Delete</th>
                    <th class="bg-secondary">Buy</th>
                </tr>
            </thead>
            <tbody>
                <!-- It helps to loop all the toys i added in addtoy.ejs-->
                <% for (let i = 0; i < toys.length; i++) { %>
                    <tr>
                        <td><%= toys[i].toyid %></td>
                        <td><a href="/inventory/<%= toys[i].toyid%>"><%= toys[i].productName %></a></td>
                        <td><img src="/images/<%= toys[i].image %>" alt="Toy Image" width="100"></td>
                        <td><%= toys[i].agegroup %></td>
                        <td><%= toys[i].quantity %></td>
                        <td><%= toys[i].price %></td>
                        <td><a href="/inventory/<%= toys[i].toyid %>/update">Edit</a></td>
                        <td><a href="/inventory/<%= toys[i].toyid %>/delete" onclick="return confirm('Are you sure you want to delete this toy?')">Delete</a></td>
                        <td>
                            <a href="/cart/add/<%= toys[i].toyid %>" class="btn btn-danger">Add to Carts</a>

                        </td>
                    </tr>
                <% } %>
                
            </tbody>
        </table>
    </div>
</body>
</html>
